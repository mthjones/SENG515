<%= link_to "Add a new workshop", new_workshop_path %>

<% for workshop in @workshops %>
<div>
	<h2><%= workshop.title %></h2>
	<p><%= workshop.description %></p>
	<%= link_to "View Workshop", workshop %>
</div>
<% end %>