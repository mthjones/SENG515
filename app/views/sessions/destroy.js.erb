// Remove the session from the list
$('.session[data-id=<%= @session.id %>]').remove();

// Make sure no sessions are active
$('#session-list li').removeClass("active");

// Reset content of well
$('#session-info').text("No session selected...");