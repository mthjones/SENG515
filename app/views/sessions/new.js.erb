// Update the session list to highlight the Add Session link
$('#session-list li').removeClass('active');
$('#add-session').addClass('active');

// Render the form into the well
$("#session-info").html($("<%=j render 'sessions/form' %>"));